name = "code-explorer"
description = "Codebase exploration specialist. Use proactively to locate definitions, trace call paths, and map unfamiliar code before designing or changing it. Read-only; returns a compact report with path:line evidence instead of raw file dumps."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
{{- template "agent_explorer_prompt" . -}}
"""
